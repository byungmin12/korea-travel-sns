# 폴더 구조

```markdown
──── korea_travel_sns_project
├── config        // 전역 설정 (WebConfig, SecurityConfig 등)
├── domain        // 핵심 도메인 모델 (엔티티, 서비스 인터페이스)
├── infrastructure// DB, 외부 API 연동
├── service   // 유스케이스, 서비스 구현
├── controller           // 컨트롤러, 요청/응답 DTO
└── common        // 공통 유틸, 에러처리, 상수
```

